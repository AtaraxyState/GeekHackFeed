<?xml version="1.0" encoding="utf-8"?>
<!-- versionCode and versionName are deliberately absent. aapt2 injects them
     from the command line only when the manifest does not already declare
     them, and build.py derives both from the release tag. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geekhackfeed">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Lets the app hand a downloaded release to the system installer. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Serves only the downloaded update, read-only, to the installer. -->
        <provider
            android:name=".UpdateProvider"
            android:authorities="com.geekhackfeed.updates"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>
</manifest>
